# BOOK-COK, 아이들에게 설명하기

> 이 프로젝트에서 실제로 일어난 일을 순서대로 정리한 것.
> 각 장은 **무슨 일이 있었나 → 이렇게 물어보세요 → 남길 한 문장** 구조다.
> 앞에서부터 읽으면 그대로 수업이 된다.

---

## 0. 우리가 받은 것

AI에게 "독서 집중 앱을 만들어줘"라고 했더니 화면이 나왔다.
홈, 독서, 기록, 보관함, 성장 — 다섯 화면에 마스코트까지 있었다.

**그런데 절반은 그림이었다.**

> **"이거 다 된 것 같아?"**
> 아이들은 대부분 "네"라고 한다. 화면이 그럴듯하니까.
> 그 대답을 기억해뒀다가 1장 끝에서 다시 물어본다.

---

## 1. 화면은 주장이고, 코드가 사실이다

### 무슨 일이 있었나

독서 화면에 이렇게 적혀 있었다.

```
🔕 알림 차단 · 📝 기록 준비 · 🎧 백색소음
```

이 중 진짜로 동작하는 건 백색소음 하나뿐이었다.
**알림을 차단하는 코드는 파일 전체에 한 줄도 없었다.**

버튼도 마찬가지였다. `NFC 태그하고 독서 시작`을 눌러 따라가 보니
`startReading()` — 화면을 바꾸고 타이머를 켜는 게 전부였다. NFC는 이름뿐이었다.

책 상세 화면은 더했다. **어떤 책을 열어도 같은 감상 3개가 떴다.**
모모를 열어도 데미안 이야기가 나왔다.

### 이렇게 물어보세요

> "이 화면에 '알림 차단'이라고 써 있지. 그럼 진짜로 알림이 꺼질까?
> 지금 친구한테 카톡 보내달라고 해봐. ... 알림 오지? 그럼 저 글씨는 뭘까?
>
> 저건 우리가 손으로 써 넣은 글씨야. HTML에 `<div>🔕 알림 차단</div>`이라고 쓰면
> 화면에 그 글자가 뜰 뿐이지, 알림이 꺼지는 게 아니야."

**실습**: 보관함에서 책 세 권을 연달아 열어보게 한다. 30초면 스스로 발견한다.
그다음 F12를 눌러 콘솔을 열게 한다. 빨간 글씨가 기다리고 있다.

### 남길 한 문장

> **화면은 주장이고, 코드가 사실이다.**

- 버튼 이름은 아무렇게나 지을 수 있다. '로켓 발사'라고 써도 된다
- 진짜 동작은 `onclick=` 뒤를 따라가야 보인다

---

## 2. 조용히 실패하는 앱

### 무슨 일이 있었나

콘솔에 이런 게 떠 있었다.

```
ReferenceError: openDB is not defined
```

`openDB()`를 부르는 코드는 있는데, **그 함수를 만들어주는 걸 빼먹은 것**이다.
AI가 긴 코드를 만들다가 한 조각을 흘렸다.

원래대로면 앱이 여기서 멈춰야 한다. 그런데 `try-catch`가 에러를 잡아서
"IndexedDB 못 쓰네, 그럼 localStorage 쓰자"로 넘어가버렸다.

**그래서 앱은 잘 돌아갔고, 아무도 문제를 몰랐다.**
화면에는 "IndexedDB에 저장합니다"라고 계속 적혀 있었다. 거짓말이 된 것이다.

### 이렇게 물어보세요

> "화면은 멀쩡한데 속에서는 실패하고 있었어. 이게 좋은 걸까 나쁜 걸까?
>
> 반반이야. 사용자 입장에선 앱이 안 죽어서 좋아.
> 근데 개발자 입장에선 버그를 못 찾게 돼.
>
> 그래서 `catch`에 들어가면 **반드시 기록을 남겨야 해.**
> 여기도 남기긴 했어. 우리가 콘솔을 안 봤을 뿐이지."

### 남길 한 문장

> **try-catch는 안전장치이자 위험장치다.**

- 에러를 잡았으면 반드시 흔적을 남길 것
- `catch(e){}` 처럼 비워두는 건 최악 — 버그가 영원히 안 보인다

---

## 3. 쉬운 걸 고르는 게 실력이다

### 무슨 일이 있었나

빠진 `openDB()`를 만들어 넣을 수도 있었다. 40~60줄이면 된다.
대신 우리는 **IndexedDB를 통째로 걷어냈다.**

계산해봤기 때문이다.

- localStorage 용량: 약 5MB
- 독서 기록 하나: 약 300바이트 (한글은 두 배로 잡아서 넉넉히)
- 5,000,000 ÷ 600 ≈ **8,000개**

한 사람이 독서 기록을 팔천 개 쓸 일은 없다.
IndexedDB는 이 앱에 **명백한 과한 기술**이었다.

### 이렇게 물어보세요

> "AI가 왜 IndexedDB를 썼을까? '저장 기능 만들어줘' 하니까
> 제일 좋은 걸 골라준 거야. 우리한테 필요한 게 아니라.
>
> **어려운 기술을 쓰는 게 실력이 아니야. 문제 크기에 맞는 걸 고르는 게 실력이야.**
> 발표 때 이걸 말하면 오히려 점수가 올라가. '왜 단순한 걸 골랐는지' 설명할 수 있으니까."

### 남길 한 문장

> **기술은 문제 크기에 맞춰 고른다. 근거는 숫자로.**

---

## 4. 같은 정보를 두 군데 적지 않는다

### 무슨 일이 있었나

홈 화면은 "이번 달 4권", 성장 화면은 "17권"이라고 했다.
**실제 저장된 기록은 0개였다.** 둘 다 손으로 적어둔 숫자였다.

우리는 화면의 모든 숫자를 저장된 기록에서 계산하게 바꿨다.
이제 숫자가 나오는 입구는 `renderAll()` 한 곳뿐이다.

그리고 "읽기 진행률 78%"는 **되살리지 않고 지웠다.**
전체 쪽수를 입력받는 곳이 없어서 계산할 방법이 아예 없었기 때문이다.

### 이렇게 물어보세요

> "홈은 4권, 성장은 17권. 어느 게 맞아? 둘 다 틀렸어.
> 왜 이렇게 됐을까? **숫자를 두 군데에 각각 적어놨기** 때문이야.
> 한 군데 고치면 다른 데를 까먹어. 그래서 어긋나.
>
> 그리고 진행률 78%는 어떻게 할까? 세 가지 선택지가 있어.
> ① 진짜로 계산한다 ② 없앤다 ③ 계산에 필요한 걸 새로 입력받는다.
> 우리는 ②를 골랐어. 근데 ③도 답이야. 뭐가 더 필요할까?"

### 남길 한 문장

> **같은 정보가 두 곳에 있으면 언젠가 반드시 어긋난다.**

- "이 숫자 어디서 왔어?"에 답할 수 있어야 한다

---

## 5. 웹이 못 하는 일이 있다 — 그리고 이유가 있다

### 먼저 쉬운 것부터: 소리

태그를 대면 백색소음이 자동으로 나오게 하려 했는데 안 됐다.
**브라우저는 사용자가 화면을 한 번 누르기 전에는 소리를 낼 수 없다.**

> "웹사이트 들어갔는데 갑자기 광고 소리가 크게 나면 어때?
> 그거 막으려고 만든 규칙이야."

그래서 소리를 **예약**해두고 첫 터치에 재생하도록 했다.

### 그다음 어려운 것: 방해금지

**웹페이지는 방해금지 모드를 켤 수 없다. 앞으로도 안 될 것이다.**

> "반대로 생각해봐. 웹사이트가 네 폰 설정을 바꿀 수 있으면 어떻게 될까?
> 이상한 사이트 하나 잘못 들어갔는데 알림이 다 꺼지고 소리가 최대로 커지면?
>
> 그래서 브라우저는 울타리 안에서만 움직여. 이걸 **샌드박스**라고 해.
> 우리를 막으려는 게 아니라 **지키려고** 있는 거야."

### 그럼 포기하나 — 여기가 핵심

웹이 못 하는 걸 **폰 자체는 할 수 있다.** 아이폰 단축어, 안드로이드 매크로.
**이미 만들어져 있는 도구다. 우리가 만들 필요가 없다.**

```
[NFC 태그]  →  [폰 자동화]            →  [BOOK-COK 웹앱]
  물리적        방해금지 ON + 주소 열기     타이머·기록·백색소음
                (OS만 할 수 있는 일)        (우리가 만든 부분)
```

### 남길 한 문장

> **안 되는 데는 이유가 있다. 우회는 다른 층에서 찾는다.**
> **좋은 개발자는 많이 만드는 사람이 아니라 뭘 안 만들지 아는 사람이다.**

- 층을 나눠 생각한다: 태그 / OS / 우리 앱
- 막히면 "이거 이미 있는 거 아닐까?"를 먼저 검색

---

## 6. 켠 것은 끈다 — 그리고 끄기가 실패할 때도 생각한다

### 무슨 일이 있었나

원래 앱은 백색소음을 켜면 **독서를 끝내기 전까지 끌 수 없었다.**
일시정지를 눌러도 라벨이 그대로라 멈춘 건지 알 수 없었다.

방해금지도 마찬가지다. 켜는 것만 만들면 **폰이 계속 방해금지로 남는다.**
밤에 부모님 전화를 못 받는 사고가 진짜로 난다.

그래서 끄는 방법을 만들었다. 그런데 여기서 한 번 더 걸렸다.

**끄기 버튼을 눌렀더니 "'독서 끝'을 찾을 수 없습니다"가 떴다.**
단축어를 아직 안 만들었기 때문이다. 버튼은 만들었는데 **그 버튼이 실패하는 경우**를 안 만든 것이다.

### 이렇게 물어보세요

> "방해금지 켜는 거 만들었지. 그럼 언제 꺼져?
> ... 안 꺼져. 우리가 안 만들었으니까.
>
> (끄기를 만든 뒤) **단축어 안 만든 애가 이 버튼 누르면 어떻게 돼?**"

아이들은 보통 '만든다'까지는 간다. '실패할 때'는 거의 생각하지 않는다.

### 남길 한 문장

> **상태를 바꾸는 기능을 만들면, 되돌리는 방법도 세트로 만든다.**
> **그리고 되돌리기가 실패할 때 빠져나갈 길까지 만든다.**

- 켜기/끄기, 로그인/로그아웃, 저장/삭제, 시작/중단

---

## 7. 만든 건 올려야 존재한다

### 무슨 일이 있었나

NFC 태그에는 **인터넷 주소**가 들어가야 한다.
`C:\Users\...\index.html` 같은 내 컴퓨터 경로는 다른 폰에서 안 열린다.

그래서 배포했다. 그런데 사고가 났다.

배포 기록에 **`found 43 files`** 라고 찍혔다. 우리 파일은 4개뿐이었다.
`.git` 폴더 안의 42개가 통째로 올라간 것이다.
`사이트주소/.git/HEAD` 를 열었더니 **진짜로 열렸다.**

원인은 배포 설정을 "폴더 전체를 올리고 뺄 것을 나열"하는 방식으로 쓴 것이었다.
`.git`이라는 폴더는 걸렀는데 **그 안쪽**은 안 걸러졌다.

### 이렇게 물어보세요

> "배포하고 나면 숫자를 세봐. 4개여야 하는데 43개면 뭔가 잘못된 거야.
> 이번엔 공개돼도 되는 내용이었어. 근데 여기 비밀번호가 있었으면?"

**실습**: 일부러 잘못된 설정으로 올려보고 `/.git/HEAD`를 열어보게 한다.

### 남길 한 문장

> **올릴 때는 "무엇을 뺄까"가 아니라 "무엇을 올릴까"로 정한다.**

- 빼는 목록은 언젠가 빠뜨린다. 올릴 목록은 안 빠뜨린다
- 그리고 **고쳤는데 폰에서 안 바뀌면 캐시부터 의심한다**

---

## 8. 확인하는 법 — 이게 제일 중요하다

여기서 우리는 **세 번** 속았다. 매번 다른 것에게.

### ① 화면이 속였다

`🔕 알림 차단` — 글씨만 있고 코드가 없었다. (1장)

### ② 증상이 속였다

"저장했는데 보관함에 안 떠요"라는 신고가 왔다.
코드를 고치려다 멈추고, **화면에 "지금 몇 개 저장돼 있는가"를 먼저 띄웠다.**

`저장된 기록 1개` — 저장은 되고 있었다.
진짜 원인은 저장한 책 이름이 이미 샘플로 있던 `데미안`이라
새 카드가 안 생기고 설명만 바뀐 것이었다. **버그가 아니었다.**

확인 장치 심는 데 5분, 원인 파악에 1분.

### ③ 확인 도구가 속였다

우리는 코드가 잘 도는지 자동으로 검사하는 도구를 만들어 썼다.
그 도구에서 "이건 아이폰이야"라고 설정했는데 — **그 설정이 무시되고 있었다.**
검사 도구가 계속 안드로이드인 척 검사하고 있었던 것이다.

새로 쓴 설명이 화면에 안 나오는 걸 보고서야 알았다.

### 이렇게 물어보세요

> "화면이 거짓말할 수 있어. 증상도 거짓말할 수 있어.
> 그럼 확인하려고 만든 도구는? **그것도 거짓말할 수 있어.**
>
> 그래서 뭘 믿어야 할까? **실제로 그 일을 해보는 것.**
> 진짜 폰으로, 진짜 태그로, 진짜 상황에서."

### 남길 한 문장

> **증상만 보고 고치기 시작하지 않는다. 먼저 확인 장치를 심는다.**
> **그리고 확인 장치도 확인한다.**

---

## 9. 공짜 선택은 없다

### 무슨 일이 있었나

우리는 서버를 쓰지 않기로 했다. 독서 기록은 개인적인 내용이니까.
좋은 선택처럼 보였다. 그런데 청구서가 왔다.

**아이폰은 홈 화면에 추가한 앱과 사파리의 저장 공간을 따로 둔다.**
사파리에서 만든 기록이 홈 화면 앱에서 안 보였다. 우리가 못 고치는 문제다.

그래서 **기록 내보내기/가져오기**를 만들었다.
서버가 없으면 옮기는 일은 사용자 몫이 되기 때문이다.

### 이렇게 물어보세요

> "'서버 안 씀 = 개인정보 안전 = 좋은 선택' 여기까지는 다 생각해.
> 근데 그 선택의 **대가**는? 다른 기기에서 못 본다는 거야.
>
> 어느 쪽이 정답일까? 정답은 없어. **선택이야.**
> 중요한 건 대가를 알고 고르는 거지."

### 남길 한 문장

> **설계에는 대가가 따른다. 공짜 선택은 없다.**

---

## 10. AI와 일하는 법

수업 중에 반복해서 말해줄 것.

**1. 한 번에 하나씩 시킨다**
세 개를 한꺼번에 시키면 어디서 깨졌는지 못 찾는다.

**2. "고쳐줘" 전에 "찾아줘"**
`이 파일에서 동작하지 않는 부분을 찾아줘. 아직 수정하지 마.`
진단을 먼저 받아야 내가 판단할 수 있다.

**3. 이해 못 한 코드는 남기지 않는다**
`이거 왜 이렇게 고쳤는지 설명해줘.`
설명을 못 알아듣겠으면 더 쉬운 방법을 요청한다.

**4. AI가 준 걸 확인 없이 믿지 않는다**
> "AI 없었으면 이만큼 못 만들었어. 화면 예쁘고 백색소음까지 나와.
> 다만 **AI는 아주 빠른 조수야. 조수가 한 일은 의사가 확인하는 거고, 의사는 너희야.**"

---

## 11. 발표

### 먼저 말할 한 문장

> **"웹으로는 방해금지를 켤 수 없다는 걸 알게 되어서,
> 휴대폰 자동화와 웹앱을 연결하는 방식을 선택했습니다."**

한계를 먼저 말하고 우회로를 설명하면 질문받을 게 없어진다.
"앱을 만들었습니다"가 아니라 **"웹앱을 만들었습니다"** 라고 말한다.

### 마지막 질문 대비

심사위원이 반드시 묻는다. **"AI로 만든 거 아닌가요?"**

이때 `고친-버그-목록.md`를 펼친다. 17개 항목이 있다.
그중 **우리가 낸 사고도 들어 있다** — 깃 폴더 공개, 캐시, 앱을 통째로 멈추게 한 실수.

남의 버그 15개보다 자기 버그 2개가 "직접 했다"는 증거로 훨씬 세다.

### 준비할 것

- `git log --oneline` 화면 (커밋이 곧 작업 기록이다)
- 태그를 대는 시연 — 방해금지가 켜지고 타이머가 도는 것을 한 동작으로
- 다시 태그해서 꺼지는 것까지

---

## 부록 · 시간이 없을 때 이것만

| 장 | 한 문장 |
|---|---|
| 1 | 화면은 주장이고 코드가 사실이다 |
| 2 | try-catch는 안전장치이자 위험장치다 |
| 3 | 기술은 문제 크기에 맞춰 고른다 |
| 4 | 같은 정보를 두 군데 적지 않는다 |
| 5 | 안 되는 데는 이유가 있다. 우회는 다른 층에서 |
| 6 | 켠 것은 끈다. 끄기가 실패할 때까지 생각한다 |
| 7 | 올릴 때는 "무엇을 올릴까"로 정한다 |
| 8 | 확인 장치도 확인한다 |
| 9 | 공짜 선택은 없다 |
