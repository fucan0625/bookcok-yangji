# 우리가 찾아서 고친 것들

> 발표 자료용. "AI로 만든 거 아닌가요?"에 대한 답이다.
> AI가 만든 프로토타입을 받아서, **어디가 거짓말인지 찾아내고 고친 기록**이다.

---

## 한 줄 요약

AI가 만든 첫 화면은 **동작하는 것처럼 보이지만 실제로는 동작하지 않는 부분**이 많았다.
우리는 그것을 하나씩 찾아내 고쳤고, 웹으로 불가능한 것은 다른 방법을 찾았다.

---

## 1. 저장이 되는 줄 알았는데 아니었다

| | |
|---|---|
| **증상** | 화면에 "IndexedDB에 실제 저장합니다"라고 적혀 있었다 |
| **찾은 방법** | F12 콘솔에 `ReferenceError: openDB is not defined` 경고 |
| **원인** | `openDB()`를 부르는 코드는 있는데 그 함수를 만들어 두지 않았다. `db`, `STORE`도 선언되지 않았다 |
| **왜 안 들켰나** | `try-catch`가 에러를 잡아서 조용히 localStorage로 넘어갔다. 화면은 멀쩡해 보였다 |
| **고친 방법** | IndexedDB를 되살리지 않고 **걷어냈다**. 기록 하나가 300바이트, localStorage 용량은 약 5MB → 수천 개를 넣어도 남는다. 이 앱에 IndexedDB는 과한 기술이었다 |
| **확인** | 콘솔에 경고가 더 이상 뜨지 않는다 |

## 2. 같은 앱인데 화면마다 다른 숫자를 말했다

| | |
|---|---|
| **증상** | 홈은 "이번 달 4권", 성장 탭은 "17권". 실제 저장된 기록은 0개 |
| **원인** | 숫자를 계산하지 않고 HTML에 손으로 적어 두었다 |
| **고친 방법** | 홈·성장·보관함·책 상세의 모든 숫자를 저장된 기록에서 계산한다. `renderAll()` 한 곳에서 전부 다시 그린다 |
| **덤** | 계산할 근거가 없는 "읽기 진행률 78%"는 되살리지 않고 삭제했다. 전체 쪽수를 입력받는 곳이 없어서 계산할 방법이 없다 |

## 3. 어떤 책을 열어도 같은 내용이 나왔다

| | |
|---|---|
| **증상** | 보관함에서 `모모`를 열면 제목만 바뀌고 `데미안`의 감상 3개가 그대로 떴다 |
| **원인** | 책 상세 화면의 기록이 HTML에 고정되어 있었다 |
| **고친 방법** | 가짜 기록을 지우고, 그 책의 실제 기록만 보여준다. 머리말도 누적 시간·기록 수·마지막 독서일로 계산 |

## 4. 보관함을 그리는 코드가 두 개였다

| | |
|---|---|
| **증상** | 완독 버튼을 누르면 카드 문구 형식이 통째로 바뀌었다 |
| **원인** | `renderLibrary`와 `renderLibraryFallback`이 같은 목록을 서로 다른 문구로 그렸다 |
| **고친 방법** | 하나로 합쳤다 |

## 5. 켜는 버튼은 있는데 끄는 버튼이 없었다

| | |
|---|---|
| **증상** | 백색소음을 켜면 독서를 종료하기 전까지 끌 수 없었다. 일시정지를 눌러도 라벨이 그대로라 지금 멈춘 건지 알 수 없었다 |
| **고친 방법** | `⏹ 소리 끄기` 추가, 일시정지 라벨을 `▶ 이어서 읽기`로 전환 |
| **원칙** | **상태를 바꾸는 기능을 만들면 되돌리는 방법도 같이 만든다** |

## 6. 저장한 뒤 폼이 비워지지 않았다

| | |
|---|---|
| **증상** | 다음 기록을 쓰려는데 방금 쓴 문장이 남아 있었다. 연달아 저장하면 같은 독서 시간이 또 더해졌다 |
| **고친 방법** | 저장 후 입력칸과 타이머를 초기화 |
| **덤** | 제목이 비어도 `'제목 없음'`으로 저장되는 바람에 빈 제목 검사가 한 번도 걸리지 않고 있었다 |

## 7. NFC는 이름뿐이었다

| | |
|---|---|
| **증상** | 버튼 이름이 "NFC 태그하고 독서 시작"인데 `onclick`을 따라가 보니 `startReading()` — 화면 전환과 타이머뿐. NFC 코드는 한 줄도 없었다 |
| **화면의 `🔕 알림 환경`도** | 글씨만 있고 알림을 끄는 코드가 없었다 |
| **고친 방법** | 아래 8번 |

## 8. 웹은 방해금지를 켤 수 없다 — 그래서 층을 나눴다

```
[NFC 태그]  →  [폰 자동화]            →  [BOOK-COK 웹앱]
  물리적        방해금지 ON + 주소 열기     타이머·기록·백색소음
                (OS만 할 수 있는 일)        (우리가 만든 부분)
```

- 브라우저는 폰 설정을 바꿀 수 없다. **막힌 게 아니라 우리를 지키려고 그렇게 만든 것**이다
  (아무 사이트나 들어갔는데 알림이 다 꺼지면 안 되니까)
- 그래서 켜는 일은 폰 자동화(iOS 단축어 / 안드로이드 매크로)에 맡기고,
  웹앱은 `?src=nfc&book=데미안` 이라는 주소로 **"자동화를 거쳐 들어왔다"는 사실만** 전달받는다
- 화면의 가짜 라벨을 지우고, **아는 사실만 말하는 배너**로 바꿨다
  - NFC로 들어옴 → "방해금지 켜짐 · NFC 자동화가 켜 두었습니다"
  - 그냥 들어옴 → "방해금지 꺼짐 · 웹앱은 방해금지를 직접 켤 수 없습니다"
- 독서를 종료하면 **끄는 방법을 안내**한다 (5번과 같은 원칙)

## 9. 소리는 자동으로 시작할 수 없었다

| | |
|---|---|
| **증상** | 태그로 들어오면 백색소음이 바로 나오게 하려 했는데 안 났다 |
| **원인** | 브라우저는 사용자가 화면을 한 번 누르기 전에 소리를 낼 수 없다. 웹사이트가 갑자기 큰 소리를 내는 걸 막기 위한 정책 |
| **고친 방법** | 소음을 **예약**해 두고 첫 터치에 재생한다. 화면에 "👆 화면을 한 번 터치하면 시작됩니다"라고 안내 |
| **8번과 같은 종류** | 웹의 한계를 만나면 → 왜 막았는지 이해하고 → 다른 층에서 해결 |

## 10. 배포하면서 우리가 낸 사고

| | |
|---|---|
| **증상** | 첫 배포 로그에 `found 43 files`. 우리 파일은 4개뿐이었다 |
| **원인** | 배포 설정을 "폴더 전체를 올리고 뺄 것을 나열"하는 방식으로 썼는데, `.git` 폴더 **안쪽**은 걸러지지 않았다 |
| **결과** | `사이트주소/.git/HEAD` 가 열렸다. 깃 저장소가 통째로 공개됐다 |
| **고친 방법** | `public/` 폴더를 만들고 **거기 있는 것만** 올리도록 바꿨다 |
| **원칙** | **"무엇을 뺄까"가 아니라 "무엇을 올릴까"로 정한다.** 빼는 목록은 언젠가 빠뜨린다 |

## 11. 눌러도 아무 일 없는 버튼

| | |
|---|---|
| **증상** | 보관함의 `＋ 책 추가`를 누르면 "다음 버전에서 연결할 수 있어요" 알림만 떴다 |
| **판단** | 기록에 새 책 제목을 적으면 그 책이 보관함에 자동으로 생긴다. **책 목록을 따로 관리할 이유가 없었다** |
| **고친 방법** | 기능을 새로 만드는 대신 버튼을 `＋ 새 기록 쓰기`로 바꿔 기록 화면으로 보낸다 |
| **원칙** | 안 되는 기능은 만들거나 없앤다. **"다음 버전에" 라고 적어두고 남기지 않는다** |

## 12. 버그인 줄 알았는데 아니었던 것

| | |
|---|---|
| **증상** | "저장했는데 보관함에 안 뜬다" |
| **확인한 것** | 보관함 맨 위에 `저장된 기록 1개`라고 떴다 → 저장은 되고 있었다 |
| **진짜 원인** | 저장한 책 이름이 샘플로 이미 있던 `데미안`이었다. 새 카드가 생기는 대신 기존 카드 설명이 `아직 기록 없음` → `1개 기록`으로 바뀌었을 뿐이다 |
| **배운 것** | **증상만 보고 고치기 시작하면 안 된다.** 화면에 "지금 몇 개 저장돼 있는가"를 띄우니 1분 만에 갈렸다 |
| **덤으로 찾은 것** | 확인하다가 index.html이 1시간 캐시로 배포되고 있는 걸 발견했다. 고쳐도 폰에 늦게 반영되는 원인이라 `no-cache`로 바꿨다 |

## 13. 아이폰에서는 앱과 사파리가 서로 다른 저장 공간을 쓴다

| | |
|---|---|
| **증상** | 사파리에서 만든 기록이 홈 화면 앱에서 안 보였다 |
| **원인** | iOS는 홈 화면에 추가한 웹앱에게 사파리와 **분리된 저장 공간**을 준다. 우리 코드 문제가 아니라 iOS 동작이다 |
| **왜 중요한가** | NFC 자동화의 '주소 열기'는 **사파리로** 열린다. 그러면 기록은 사파리에 쌓이는데 홈 화면 아이콘을 누르면 빈 앱이 열린다 |
| **고친 방법** | 서버를 쓰지 않기로 했으므로, 옮기는 수단을 만들었다. 보관함의 **기록 내보내기 / 가져오기** |
| **원칙** | **설계에는 대가가 따른다.** "서버를 안 쓴다"를 선택하면 "기기 사이 이동은 사용자가 한다"가 따라온다. 공짜 선택은 없다 |

## 14. 기능을 붙이다가 앱을 통째로 죽였다

| | |
|---|---|
| **증상** | 설정 화면을 추가했더니 **화면에 아무것도 안 떴다** |
| **원인** | 화면을 그리는 `renderAll()`이 `const SHORTCUT_LINKS` 선언보다 **먼저** 실행됐다 |
| **왜 어려운가** | 문법 검사는 통과한다. 문법은 맞기 때문이다. 실제로 실행해봐야 드러난다 |
| **배운 것** | `function`은 어디에 적든 위로 끌어올려지지만, `const`는 **적힌 자리에 도달해야** 생긴다 |
| **고친 방법** | 화면 그리는 코드를 스크립트 맨 끝에서 부르고, 이유를 주석에 남겼다 |
| **1번과 비교** | 1번은 AI가 낸 버그, 이건 **우리가 기능을 추가하다 낸 버그**다. 그리고 이번엔 `try-catch`가 없어서 조용히 넘어가지 않고 앱이 아예 멈췄다. 어느 쪽이 나을까? |

## 15. 같은 거짓말을 다시 했다

| | |
|---|---|
| **증상** | 태그에 주소만 써 넣은 경우에도 앱이 "방해금지 켜짐"이라고 표시했다 |
| **왜 생겼나** | 처음엔 자동화로만 들어온다고 생각해서 `?src=nfc`면 켜진 것으로 봤다. 그런데 책마다 태그를 붙이려면 **태그에 주소를 쓰는 방식**이어야 했고, 그 태그는 방해금지를 못 켠다 |
| **무엇이 문제인가** | **1차시에 지적한 `🔕 알림 환경` 라벨과 똑같은 거짓말**이다. 원칙을 배워놓고 새 기능을 붙이다 재발시켰다 |
| **고친 방법** | "태그로 들어왔다"(`src=nfc`)와 "방해금지가 켜졌다"(`dnd=on`)를 분리. 배너가 세 가지 상태를 정확히 말한다 |
| **배운 것** | **원칙은 한 번 배우고 끝이 아니다.** 새 기능을 붙일 때마다 화면이 다시 거짓말을 시작한다 |
| **어떻게 찾았나** | "태그에 주소 쓰기가 필수야?"라는 질문에서. **질문 하나가 코드 검사보다 낫다** |

---

## 발표 때 먼저 말할 것

> **"웹으로는 방해금지를 켤 수 없다는 걸 알게 되어서,
> 휴대폰 자동화와 웹앱을 연결하는 방식을 선택했습니다."**

한계를 먼저 말하고 우회로를 설명하면 질문받을 게 없어진다.
"앱을 만들었습니다"가 아니라 **"웹앱을 만들었습니다"** 라고 말한다.

## 예상 질문

| 질문 | 답 |
|---|---|
| 앱인가요, 웹인가요? | 웹앱입니다. NFC와 방해금지는 폰 자동화 기능과 연결했습니다 |
| 데이터는 어디 저장되나요? | 사용자 브라우저에 저장됩니다. 서버는 쓰지 않습니다 |
| 다른 기기에서도 보이나요? | 안 보입니다. 서버를 쓰지 않기로 해서 생긴 대가입니다. 대신 기록을 내보내고 가져오는 기능을 넣었습니다 |
| 왜 서버를 안 썼나요? | 독서 기록은 개인적인 내용이라 밖으로 안 나가는 게 낫다고 판단했습니다 |
| 아이폰에서도 되나요? | 됩니다. 단축어 앱으로 설정하는 방식이 다를 뿐입니다 |
| 백색소음 4개가 비슷하게 들려요 | 맞습니다. 화이트노이즈에 필터만 다르게 건 것입니다. 음원 파일 없이 코드로 소리를 만들어서 저작권 문제가 없고 용량이 0입니다 |
| AI로 만든 거 아닌가요? | AI로 초안을 만들고, **동작하지 않는 부분을 찾아서 고쳤습니다** → 이 문서의 1~15번을 보여준다 |
