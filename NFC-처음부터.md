# NFC 태그, 처음부터

> NFC를 한 번도 안 다뤄본 사람 기준. 학생에게 그대로 줘도 된다.

---

## 1. NFC 태그가 뭔가

**교통카드 안에 든 것과 같은 물건이다.**

- 배터리가 없다. 폰을 가까이 대면 폰이 전기를 보내주고, 그 힘으로 깨어나 글자를 뱉는다
- 아주 짧은 글자만 저장한다 (우리는 **인터넷 주소 한 줄**만 넣을 것이다)
- 스티커·카드·키링 모양으로 판다. 스티커를 책 뒤표지나 책상에 붙인다
- 몇 번이고 다시 쓸 수 있다 (잠그기 전까지는)

**우리가 하려는 것:** 태그에 주소 한 줄을 써 넣고, 폰을 대면 그 주소가 열리게 한다.

## 2. 태그 사기

- 검색어: **NFC 스티커 NTAG213**
- 개당 수백 원. 10개 묶음이 흔하다. **학생 수 + 여유분 3~4개**
- NTAG213이면 충분하다 (우리 주소는 100바이트가 안 된다)
- **철제 책상에 붙일 거면 "금속용(on-metal) NFC 태그"를 따로 사야 한다.**
  일반 태그는 금속 위에서 안 읽힌다. 책 표지는 상관없다

## 3. 아이폰만 쓴다면 — NFC Tools 없이도 된다

아이폰의 **단축어** 앱은 태그를 **지문처럼 ID로 인식**한다.
태그 안에 뭐가 쓰여 있든 상관없이 "이 태그를 대면 이걸 해라"를 정할 수 있다.

즉 **태그를 산 그대로(빈 태그) 써도 된다.** NFC Tools는 필요 없다.

> 그럼 NFC Tools는 언제 쓰나?
> - 안드로이드 폰에서 **앱 설치 없이** 태그만 대면 열리게 하고 싶을 때
> - 태그가 불량인지 확인하고 싶을 때
> - 한 태그를 여러 사람이 각자 폰으로 쓰게 하고 싶을 때

수업에 안드로이드 학생이 있다면 태그에 주소를 써 두는 게 좋다. → 4번으로.

## 4. NFC Tools 사용법

### 설치

앱스토어/플레이스토어에서 **`NFC Tools`** 검색. 만든 곳은 **wakdev**. 무료.
(비슷한 이름의 앱이 많으니 만든 곳을 확인할 것)

### 화면 구조

앱을 열면 **아래쪽에 탭 4개**가 있다.

| 탭 | 하는 일 |
|---|---|
| **읽기 (Read)** | 태그에 뭐가 들었는지 본다 |
| **쓰기 (Write)** | 태그에 글자를 써 넣는다 ← 우리가 쓸 것 |
| **기타 (Other)** | 태그 지우기, 잠그기 |
| **작업 (Tasks)** | 안 쓴다 |

### 4-1. 먼저 읽어본다 (태그가 살아 있는지 확인)

쓰기 전에 이걸 꼭 해본다. **폰에 태그를 어디에 대야 하는지 익히는 과정이기도 하다.**

1. **읽기** 탭 → 가운데 **`스캔(SCAN)`** 버튼 누르기
2. 아이폰이면 "스캔 준비됨" 같은 창이 뜬다
3. 태그를 폰 뒷면에 댄다
   - **아이폰**: 뒷면 **맨 위쪽**, 카메라 근처
   - **안드로이드**: 뒷면 중앙~위쪽 (기종마다 다르다. 위아래로 천천히 훑는다)
4. 삑 하고 태그 정보가 뜬다 → **성공.** 종류(NTAG213)와 용량이 보인다

> 아무 반응이 없으면: 위치를 바꿔가며 다시 / 폰 케이스가 두꺼우면 빼기 /
> 안드로이드는 설정에서 NFC가 켜져 있는지 확인

### 4-2. 주소 써 넣기

1. **쓰기** 탭 → **`레코드 추가 (Add a record)`**
2. 목록이 길게 나온다. 그중 **`URL/URI`** 를 찾아 누른다
   (Text, 검색, 이메일, 전화... 중에 URL 항목)
3. 입력창에 주소를 붙여넣는다

```
https://bookcok-yangji.web.app/?src=nfc&book=%EB%8D%B0%EB%AF%B8%EC%95%88
```

   > 뒤의 `%EB%8D%B0...`는 **데미안**을 컴퓨터가 읽는 방식으로 바꾼 것이다.
   > 한글을 그대로 넣으면 깨질 수 있어서 이렇게 쓴다.
   > 책 이름 없이 `https://bookcok-yangji.web.app/?src=nfc` 만 써도 된다.

4. 확인을 누르면 목록에 한 줄이 생긴다
5. 아래 **`쓰기 (Write / N Bytes)`** 버튼 누르기
6. 태그를 폰에 댄다 (4-1에서 익힌 그 위치)
7. **"쓰기 완료"** 가 뜨면 끝

### 4-3. 잘 됐는지 확인

**읽기** 탭에서 다시 스캔해본다. 방금 쓴 주소가 보이면 성공이다.

### ⛔ 절대 누르지 말 것

**기타(Other) 탭의 `태그 잠금(Lock tag)`.**
한 번 누르면 그 태그는 **영원히** 못 고친다. 되돌릴 방법이 없다.
학생들에게 미리 말해둘 것.

## 5. 실제로 태그해보기

앱을 닫고, **폰 잠금을 푼 상태(홈 화면)** 에서 태그를 댄다.

| | 일어나는 일 |
|---|---|
| **안드로이드** | 크롬이 바로 열린다 |
| **아이폰** | 화면 위에 배너가 뜬다 → **배너를 손으로 눌러야** 사파리가 열린다 |

**성공 신호:** 독서 화면이 열리고 위쪽에 **초록색 "🔕 방해금지 켜짐"** 배너와 책 제목.

주황색 "방해금지 꺼짐"이 뜨면 주소에서 `?src=nfc` 부분이 빠진 것이다.

## 6. 여기까지는 방해금지가 안 켜진다

**중요.** 5번까지 하면 주소가 열릴 뿐이다. 방해금지는 안 켜진다.

웹페이지는 폰 설정을 못 바꾸기 때문이고, 그게 이 수업의 핵심이다.
방해금지까지 연결하려면 `폰-자동화-설정.md`의 단축어/매크로 설정이 필요하다.

```
[NFC 태그]  →  [폰 자동화]            →  [BOOK-COK 웹앱]
  주소 전달     방해금지 ON + 주소 열기     타이머·기록·백색소음
                (OS만 할 수 있는 일)        (우리가 만든 부분)
```

## 7. 안 될 때

| 증상 | 볼 것 |
|---|---|
| 태그가 아예 안 읽힘 | 대는 위치 / 두꺼운 케이스 / 금속 위인지 / NFC 설정(안드로이드) |
| 배너는 뜨는데 안 열림 | 배너를 손으로 눌러야 한다 (아이폰) |
| 주황색 배너가 뜬다 | 주소에 `?src=nfc`가 없다 |
| 책 이름이 깨진다 | 인코딩된 주소로 다시 쓰기 |
| 옛날 화면이 열린다 | 캐시. 탭을 닫았다 다시 열기 |
| 잠금화면에서 안 된다 | 잠금을 풀어야 동작한다 (정상) |
| 쓰기가 실패한다 | 이미 잠긴 태그일 수 있다. 다른 태그로 시도 |
