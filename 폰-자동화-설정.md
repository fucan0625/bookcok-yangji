# NFC 태그 ↔ 방해금지 연결 설정

## 역할 분담 (왜 이렇게 나누는가)

```
[NFC 태그]  →  [폰 자동화]              →  [BOOK-COK 웹앱]
  물리적        방해금지 ON + 주소 열기        타이머·메모·기록·백색소음
                (OS만 할 수 있음)              (우리가 만든 부분)
```

웹페이지는 방해금지 모드를 켤 수 없다. 브라우저가 폰 설정을 바꾸지 못하게
막아 두었기 때문이다(샌드박스). 그래서 **켜는 일은 폰에게 넘기고, 웹앱은
"자동화를 거쳐 들어왔다"는 사실만 URL로 전달받는다.**

## 정해진 방식 (2026-08-30)

**태그는 시작에만 쓴다. 마치는 것은 화면의 `독서 종료 → 기록` 버튼이다.**

| | 시작 | 종료 |
|---|---|---|
| **아이폰** | 태그 → 방해금지 켜짐 + 앱 열림 | 버튼 → 확인창 한 번 → `독서 끝` 이 끔 |
| **안드로이드** | 태그 → 방해금지 켜짐 + 앱 열림 | 버튼 → 기록 화면. **방해금지는 손으로 끈다** |

아이폰에서 버튼을 누르면 사파리가 "단축어 앱으로 이동하시겠습니까?"를 한 번 묻는다.
**이건 없앨 수 없다.** 웹페이지가 다른 앱으로 넘어갈 때 애플이 항상 묻기 때문이다.

확인창 없이 끄고 싶으면 **끝 태그**를 하나 더 붙이는 수밖에 없다 (아래 선택 항목).
태그의 자동화는 사파리를 거치지 않아서 아무것도 묻지 않는다.

## 권장 구성: 책상 태그 1개

학생 한 명당 태그 하나를 자기 책상(또는 개인 책갈피)에 붙인다.
폰에 자동화를 한 번 등록하면 **태그를 대는 순간 방해금지가 켜지고 앱이 열린다.**

아이폰 자동화는 태그마다 등록해야 하므로, 책마다 태그를 붙이면 학생당 등록 횟수가
책 권수만큼 늘어난다. 그래서 태그는 하나로 하고, 읽을 책은 앱에서 고른다.

## 태그에 무엇을 써야 하나

**아이폰은 아무것도 안 써도 된다.** 자동화는 태그의 **고유번호**만 읽는다.
빈 스티커 그대로 스캔해서 등록하면 되고, 주소는 단축어 안에 들어 있다.
(그래서 교통카드·출입카드로도 등록할 수 있다. 스티커가 오기 전 테스트에 쓴다)

**안드로이드는 태그에 주소를 써야 한다.** 자동화 앱 없이 크롬을 여는 방식이라
태그가 주소를 들고 있어야 한다.

```
https://bookcok-yangji.web.app/?src=nfc&book=데미안
```

| 값 | 뜻 |
|---|---|
| `src=nfc` | 자동화를 거쳐 들어왔다 → 독서 화면 자동 시작, 방해금지 켜짐으로 표시 |
| `book=데미안` | 이 태그가 붙은 책 이름 → 제목이 자동으로 채워짐 (생략 가능) |

태그마다 `book=` 값을 다르게 쓰면 **책갈피마다 다른 책**이 된다.
주소창에 직접 쳐 넣어도 똑같이 동작하므로, 태그 없이도 테스트할 수 있다.

## 스티커 사본 만들기 (여러 장으로 늘릴 때)

**아이폰용 스티커에는 복사할 내용이 없다.** 완성한 그 스티커도 속은 빈 스티커
그대로다. 자동화가 태그의 고유번호를 기억하는 것이라, 사본은 스티커가 아니라
**폰의 등록**이다.

| 상황 | 할 일 |
|---|---|
| 스티커 9장을 학생 9명에게 | 스티커는 빈 채로 나눠준다. **폰마다** 단축어 2개 + 자동화 1개(자기 스티커 스캔) |
| 한 폰에서 스티커 여러 장 | 단축어는 그대로 두고 **자동화만 태그 수만큼** 등록 (전부 `독서 시작` 을 가리키게) |

자동화는 iOS 가 공유를 막아 폰마다 직접 등록해야 한다. **단축어는 공유가 된다** —
단축어 앱에서 `독서 시작` · `독서 끝` 을 공유 → 링크 복사 하면 iCloud 링크가 나온다.
그 링크를 앱의 `SHORTCUT_LINKS` (public/index.html) 에 넣으면 설정 화면에
**'받기' 버튼**이 생겨, 새 폰 설정이 "링크 두 번 탭 + 자동화 1개"로 줄어든다.

새 폰 하나당 순서 (앱의 ⚙️ 설정 화면이 같은 내용을 안내한다):

1. `독서 시작` · `독서 끝` 단축어 받기 (링크가 있으면 탭 두 번, 없으면 손으로 만들기)
2. 자동화 → NFC → **자기 스티커** 스캔 → 동작 `단축어 실행` → `독서 시작`
3. **'실행 전 확인' 끄기**
4. 태그해서 확인: 방해금지 켜짐 + 앱 열림 → `독서 종료 → 기록` 버튼까지

불량 스티커 선별 — 자동화 등록 화면이 곧 판독기다:
단축어 → 자동화 → + → NFC → **스캔** → 스티커에 대본다.
**이름 입력창이 뜨면 정상**이고, 등록하지 않고 취소하면 된다.

NFC Tools 로 확인할 때 주의:
- **읽기 버튼을 먼저 누르고**, "스캔 준비 완료" 시트가 뜬 다음에 댄다
- 대는 곳은 뒷면 중앙이 아니라 **위쪽 모서리(카메라 근처)**, 2~3초 가만히
- 방금 태그로 자동화가 돌았다면 NFC 가 쉬는 중이다. **화면을 껐다 켜고** 다시
- 성공해도 아무것도 안 열린다. **"레코드 없음 / 빈 태그" + 고유번호**가 뜨면 그게 성공이다
- 폰에 등록된 자동화가 돈다는 것 자체가 그 스티커가 정상이라는 뜻이다

## 아이폰 (단축어 앱)

**1. `독서 시작` 단축어 (방해금지 켜기)**
- 단축어 앱 → `+` → **집중 모드 설정** 추가 → 방해금지 → **켬**
- **URL 열기** 추가 → `https://bookcok-yangji.web.app/?src=nfc&dnd=on`
- 이름을 `독서 시작`으로 저장

**2. `독서 끝` 단축어 (방해금지 끄기)**
- **집중 모드 설정** → 방해금지 → **끔**
- **URL 열기** 추가 → `https://bookcok-yangji.web.app/?done=1`
- 이름을 `독서 끝`으로 저장

> 집중 모드 설정에는 `켬`과 `끔`만 있고 **`전환`이 없다.**
> 그래서 단축어를 두 개로 나눈다.

**3. 자동화로 태그에 연결**
- 단축어 → 자동화 → 새로 만들기 → **NFC** → 스캔 → 책상 태그를 폰에 대기 → 이름 지정
- 동작: **단축어 실행** → `독서 시작`
- **"실행 전 확인"을 끈다**

> 태그는 항상 `독서 시작`만 실행한다. 켜기는 항상 켜기라 상태가 어긋나지 않는다.
> 두 번째로 태그하면 앱이 "읽던 중이었네" 하고 독서를 끝낸다.
> 다만 **방해금지는 그때 안 꺼진다.** 아래 "종료 버튼을 눌렀을 때만" 을 볼 것.


> 백그라운드 NFC 자동화는 iPhone XS 이후 기종에서 동작한다.

### 연달아 태그하면 두 번째가 안 읽힌다

아이폰의 백그라운드 NFC 읽기는 한 번 읽고 나면 잠깐 쉰다. 그래서 독서를 마치려고
바로 다시 대면 반응이 없고, **화면을 껐다 켜야** 다시 읽는다. 우리가 고칠 수 있는
부분이 아니다 (웹앱은 NFC 를 건드리지 못한다).

그래서 이렇게 안내한다.

- **시작은 태그로, 종료는 화면의 `독서 종료 → 기록` 버튼으로.**
- 재태그 종료도 되지만 의지할 동작은 아니다.

### 방해금지는 종료 버튼을 눌렀을 때만 꺼진다

사파리는 **사용자가 직접 누른 것**이 아니면 다른 앱으로 넘어가지 못하게 막는다.
그래서 앱은 `독서 종료` 버튼을 누른 **그 자리에서** `독서 끝` 을 부른다.

한동안 `setTimeout(runEndShortcut, 400)` 으로 0.4초 미뤄서 불렀는데, 그 0.4초 때문에
"누른 것"이 아니게 되어 사파리가 막았다. 미루면 안 된다.

재태그로 끝났을 때는 누른 것이 아니므로 앱이 부르지 않고 화면에 **방해금지 끄기**
버튼을 띄운다. 그걸 누르면 된다.

> 한때 "끄기를 아예 없애고 켤 때 끝날 시각을 정하자"는 안까지 갔다. 그러면 집에서
> 읽을 때 시각이 어긋난다. 증상을 없애는 길이었고, 원인은 따로 있었다.

### 동작은 딱 두 줄이어야 한다

`독서 끝` 단축어는 이 두 줄이 전부다. 세 번째 줄이 있으면 지운다.

```
1  집중 모드 설정 : 방해금지 모드 · 끔
2  https://bookcok-yangji.web.app/?done=1  열기
```

**`중단 및 출력` 을 넣으면 안 된다.** 실제로 여기서 막혔다. 이 동작은 단축어를
끝내면서 결과를 밖으로 내보내는데, 그러면 실행할 때마다 아이폰이 이렇게 묻는다.

> `독서 끝` 단축어가 1개의 텍스트 항목을(를) 출력하도록 허용하겠습니까?

세 번째 줄 오른쪽 `⊗` 를 눌러 지우면 사라진다.

`URL` 과 `URL 열기` 도 다른 동작이니 같이 조심한다. 동작 검색에 `URL` 을 치면
비슷한 게 여러 개 나오는데, 줄 끝에 **열기** 가 붙은 것이 맞다.
그냥 `URL` 은 주소를 값으로 들고만 있어서 주소가 열리지 않는다.

### 이름이 한 글자만 달라도 안 된다

앱은 `shortcuts://run-shortcut?name=독서 끝` 으로 단축어를 **이름으로** 부른다.
그래서 이름이 정확해야 한다.

| 앱이 부르는 이름 | 언제 |
|---|---|
| `독서 시작` | 웹앱의 "방해금지 켜기" 버튼 (`DND_ON_SHORTCUT`) |
| `독서 끝` | `독서 종료 → 기록` 버튼을 눌렀을 때 (`DND_OFF_SHORTCUT`) |

`독서끝`(붙여쓰기), `독서 종료`, 뒤에 딸려 들어간 공백 모두 실패한다.
실패하면 아이폰이 **"단축어를 찾을 수 없습니다"** 를 띄운다.

`독서 끝` 을 안 만들면 **종료 버튼을 눌러도 방해금지가 안 꺼진다.**
두 개 다 만들어야 한다. 이름을 바꾸고 싶으면 `public/index.html` 의
`DND_ON_SHORTCUT` / `DND_OFF_SHORTCUT` 값도 같이 바꾼다.

## 안드로이드

**태그 쓰기**: `NFC Tools` 앱 → 쓰기 → URL/URI → 위 주소 입력 → 태그에 쓰기
잠금 해제 상태에서 태그를 대면 크롬이 그 주소를 연다. (자동화 앱 없이도 여기까지는 된다)

**방해금지 켜기**: `MacroDroid` 또는 `Automate`
- 트리거: NFC 태그 → 동작: 방해금지 ON + 웹페이지 열기
- 최초 1회 **방해금지 접근 권한**을 앱에 허용해야 한다.

**끄기**: 화면 위에서 아래로 쓸어내려 방해금지 아이콘을 누른다.
(웹앱이 종료 화면에서 이 안내를 띄운다)

## 배포

태그에는 **인터넷 주소(HTTPS)** 가 들어가야 한다.
`C:\Users\...\index.html` 같은 내 컴퓨터 경로는 다른 폰에서 열리지 않는다.
현재 주소: **https://bookcok-yangji.web.app**

고친 내용을 다시 올릴 때는 `public/index.html`을 고친 뒤:

```
firebase deploy --only hosting
```

배포되는 것은 `public/` 폴더 안의 파일뿐이다. 수업 문서나 깃 기록은 올라가지 않는다.

## NFC Tools로 태그 쓰기·테스트

### 준비물

- NFC 스티커 태그 **NTAG213** (가장 흔하고 저렴). 우리 주소는 100바이트가 안 되므로 용량 충분
- 앱: **NFC Tools** (wakdev) — 아이폰·안드로이드 모두 있음, 무료
- 아이폰은 iPhone 7 이상이면 쓰기 가능

### 1단계 — 태그가 읽히는지 먼저 확인

쓰기 전에 태그가 살아 있는지부터 본다.

1. NFC Tools 열기 → **읽기(Read)** 탭 → `스캔` 누르기
2. 태그를 폰에 댄다
   - **아이폰**: 화면 맨 위쪽(카메라 근처) 뒷면
   - **안드로이드**: 뒷면 중앙~윗부분 (기종마다 다름, 위아래로 천천히 훑기)
3. 태그 종류와 용량이 뜨면 정상

> 안 읽히면 위치를 바꿔가며 다시. **금속 위에 붙은 태그는 안 읽힌다.**
> 철제 책상에 붙일 거면 금속용(on-metal) 태그가 따로 필요하다.

### 2단계 — 주소 쓰기

1. **쓰기(Write)** 탭 → `레코드 추가` → **URL/URI** 선택
2. 주소 입력 (한글이 깨질 수 있으니 아래 인코딩된 주소를 쓰는 게 안전하다)

| 책 | 태그에 넣을 주소 |
|---|---|
| 데미안 | `https://bookcok-yangji.web.app/?src=nfc&book=%EB%8D%B0%EB%AF%B8%EC%95%88` |
| 아몬드 | `https://bookcok-yangji.web.app/?src=nfc&book=%EC%95%84%EB%AA%AC%EB%93%9C` |
| 모모 | `https://bookcok-yangji.web.app/?src=nfc&book=%EB%AA%A8%EB%AA%A8` |
| 어린 왕자 | `https://bookcok-yangji.web.app/?src=nfc&book=%EC%96%B4%EB%A6%B0%20%EC%99%95%EC%9E%90` |
| 책 이름 없이 | `https://bookcok-yangji.web.app/?src=nfc` |

3. `쓰기` 누르고 태그를 댄다 → "쓰기 완료" 표시

> **`태그 잠금(Lock)`은 절대 누르지 말 것.** 한 번 잠그면 영영 못 고친다.
> 수업이 다 끝나고 확정된 뒤에만 쓴다.

### 3단계 — 태그 테스트

폰 잠금을 풀고(홈 화면 상태) 태그를 댄다.

| | 일어나는 일 |
|---|---|
| **안드로이드** | 크롬이 바로 열린다 |
| **아이폰** | 화면 위에 배너가 뜬다 → **배너를 탭해야** 사파리가 열린다 |

**성공 신호:** 독서 화면이 뜨고, 위에 **초록색 "🔕 방해금지 켜짐"** 배너와 책 제목이 보인다.
주황색 "방해금지 꺼짐"이 뜨면 `?src=nfc`가 주소에 안 들어간 것이다.

### 4단계 — 방해금지까지 연결

3단계까지는 **주소만 열릴 뿐 방해금지는 안 켜진다.** 그건 폰 자동화의 몫이다.

- **아이폰**: 위의 "아이폰 (단축어 앱)" 절대로 자동화를 만든다.
  단축어 자동화는 태그를 **ID로 인식**하므로, 태그에 URL을 안 써도 동작한다.
  (URL도 같이 써 두면 안드로이드 폰에서도 쓸 수 있어 편하다)
- **안드로이드**: MacroDroid/Automate에서 NFC 트리거 사용

> 아이폰에서 단축어 자동화와 URL 레코드를 **둘 다** 걸어두면 어느 쪽이 먼저인지
> 기기·버전에 따라 다를 수 있다. 실제로 태그해보고 확인할 것.

### 안 될 때

| 증상 | 볼 것 |
|---|---|
| 태그가 안 읽힘 | 폰의 NFC 위치 / 금속 위인지 / NFC 설정이 켜져 있는지(안드로이드) |
| 배너는 뜨는데 안 열림 | 배너를 탭해야 한다(아이폰) |
| 주소는 열리는데 주황 배너 | 주소에 `?src=nfc`가 빠졌다 |
| 책 이름이 깨져 나옴 | 인코딩된 주소로 다시 쓰기 |
| 옛날 화면이 열림 | 캐시. 탭 닫았다 열기 |
| 잠금화면에서 안 됨 | 잠금을 풀어야 동작한다 |

## 아이폰 주의: 홈 화면 앱과 사파리는 저장 공간이 다르다

iOS는 홈 화면에 추가한 웹앱에게 사파리와 분리된 저장 공간을 준다.
단축어의 'URL 열기'는 **사파리로** 열리므로, 기록은 사파리에 쌓인다.

- **한 곳만 쓰는 것을 권한다.** NFC를 쓴다면 사파리가 본체다
- 이미 양쪽에 기록이 나뉘었다면 보관함의 **기록 내보내기 / 가져오기**로 합칠 수 있다

## 확인 순서

1. 폰 브라우저에서 `주소/?src=nfc&book=데미안` 을 직접 열어본다
   → 독서 화면이 뜨고, 제목이 "데미안"으로 채워지고, 위에 초록색 "방해금지 켜짐" 배너
2. 화면을 한 번 터치 → 백색소음 재생
3. "독서 종료 → 기록" 버튼 → 방해금지가 꺼지는지 (아이폰은 `독서 끝` 이 실행된다)
4. 그다음에 태그를 연결한다
