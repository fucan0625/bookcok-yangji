# BOOK-COK 수업 가이드 · 보강판

> 기존 `BOOK-COK_수업_설명가이드.md`를 **대체하지 않는다.** 원본의 구조(진단 → 저장 → NFC → 마무리)는 그대로 두고,
> 실제로 만들어보면서 새로 나온 재료를 차시별로 어디에 끼울지 정리한 것이다.
> 코드와 커밋: `workspace/bookcok` · 배포: https://bookcok-yangji.web.app

---

## 실제로 만든 결과

| | |
|---|---|
| 배포 주소 | https://bookcok-yangji.web.app |
| NFC 태그용 주소 | `https://bookcok-yangji.web.app/?src=nfc&book=책이름` |
| 커밋 수 | 16개 (커밋 하나 = 고친 것 하나) |
| 고친 항목 | `고친-버그-목록.md` 13개 |

**커밋 로그가 곧 발표 자료다.** `git log --oneline`을 띄워놓고 발표해도 된다.

---

## 원본 가이드에 없던 새 재료 6개

원본은 4차시를 잘 짚었지만, 실제로 만들어보니 아래가 더 나왔다.
전부 **직접 겪은 것**이라 설득력이 다르다.

### ① 브라우저는 소리도 마음대로 못 낸다 (3차시, 방해금지보다 **먼저**)

"태그하면 백색소음이 자동으로"를 구현하려다 막혔다. 자동재생 정책 때문이다.
→ 소음을 **예약**해 두고 첫 터치에 재생하도록 했다.

**왜 방해금지보다 먼저인가:** 방해금지는 "왜 막았는지"가 추상적이다.
자동재생은 즉시 이해한다 — *"웹사이트 들어갔는데 갑자기 광고 소리 크게 나면 어때?"*
같은 사고(한계 → 이유 이해 → 다른 방법)를 쉬운 것으로 한 번 돌린 뒤 방해금지로 넘어간다.

### ② 켠 것을 끄는 방법에는 **실패하는 경우**도 있다 (3차시, 3-2 확장)

원본 3-2는 "켜는 걸 만들면 끄는 것도 만든다"까지다. 실제로는 한 단계 더 있었다.

1. 켜는 것만 만들었다 → 끌 방법이 없다
2. 끄는 버튼을 만들었다 → **그 버튼이 실패하는 경우를 안 만들었다**
3. 실패해도 빠져나갈 길을 만들었다

수업에서 실제로 일어난 일: 방해금지 해제 버튼을 눌렀더니
"'독서 끝'을 찾을 수 없습니다" — 단축어를 아직 안 만들었기 때문이다.
아이들은 보통 2까지 간다. **"단축어 안 만든 애가 이 버튼 누르면 어떻게 돼?"** 한 마디면 3으로 간다.

### ③ 올릴 때는 "무엇을 뺄까"가 아니라 "무엇을 올릴까" (4차시, 배포)

첫 배포에서 `.git` 폴더 42개 파일이 통째로 공개됐다. `사이트주소/.git/HEAD`가 열렸다.
"폴더 전체를 올리고 뺄 것을 나열"하는 설정을 썼는데 `.git` **안쪽**이 안 걸러진 것.

- 실습: 일부러 잘못된 설정으로 올리고 `/.git/HEAD`를 열어보게 한다
- 배포 직후 **파일 개수를 세는 습관**. 4개여야 하는데 43개면 뭔가 잘못된 것
- 이번엔 공개돼도 되는 내용이었지만, API 키가 있었으면 유출이다

### ④ 고쳤는데 폰에서 안 바뀐다 (4차시, 배포)

파이어베이스 기본 설정이 `Cache-Control: max-age=3600`이라 폰이 1시간 동안 옛 파일을 썼다.
→ `no-cache`로 바꿨다.

서비스워커를 일부러 안 넣었는데(같은 문제를 피하려고) HTTP 캐시에서 똑같은 게 나왔다.
**"안 바뀌면 캐시부터 의심"** — 수업 중 가장 자주 터질 사고다.

### ⑤ 증상만 보고 고치기 시작하지 않는다 (2차시 또는 1차시)

"저장했는데 보관함에 안 뜬다"는 신고가 들어왔다.
코드를 고치려다 멈추고 **화면에 "지금 몇 개 저장돼 있는가"를 먼저 띄웠다.**

→ `저장된 기록 1개`. 저장은 되고 있었다.
→ 진짜 원인: 저장한 책 이름이 샘플로 있던 `데미안`이라 새 카드가 안 생기고 설명만 바뀐 것.

확인 장치 심는 데 5분, 원인 파악에 1분. **버그가 아니었다.**
1-3("콘솔은 앱의 속마음")의 실전판이고, 아이들이 가장 많이 하는 실수다.

### ⑥ 설계에는 대가가 따른다 (4차시, 발표 준비)

아이폰은 홈 화면 웹앱과 사파리에 **분리된 저장 공간**을 준다. 우리가 못 고친다.
NFC 자동화는 사파리로 열리므로 기록이 두 곳으로 갈린다.

앞의 것들과 성격이 다르다. **고칠 수 있는 문제가 아니라, 우리 선택의 청구서다.**

> "서버를 안 쓴다"를 고르면 → "기기 사이 이동은 사용자가 한다"가 따라온다.

아이들은 "서버 안 씀 = 안전 = 좋은 선택"까지만 생각한다. 공짜 선택은 없다는 걸 여기서 배운다.
→ 기록 내보내기/가져오기를 만들어 대응했다.

---

## 차시별 배치

### 1차시 — 진단

| 활동 | 재료 |
|---|---|
| 책 상세를 세 권 연달아 열기 | 어떤 책을 열어도 데미안 감상이 뜬다. **F12 없이 눈으로 잡힌다** |
| 홈 카드 vs 보관함 카드 비교 | 같은 책을 "기록 5개"와 "샘플 책"으로 다르게 설명한다 |
| `onclick` 따라가기 | NFC 버튼 → `startReading()` → 화면 전환과 타이머뿐 |
| F12 콘솔 열기 | `openDB is not defined` |
| "동작하는 것 / 글씨만 있는 것" 표 만들기 | |

**남길 원칙:** 화면은 주장이고, 코드가 사실이다.

### 2차시 — 저장

| 활동 | 재료 |
|---|---|
| try-catch가 에러를 삼키는 구조 읽기 | 원본 2-1 |
| 용량 계산 → IndexedDB 걷어내기 판단 | 5MB ÷ 300바이트. **UTF-16이라 실제론 절반**이므로 8,000개로 잡는 게 안전 |
| 렌더러가 두 개인 것 발견 | 완독 버튼을 누르면 카드 문구가 바뀐다 |
| 숫자를 실제 기록과 연결 | 홈 4권 vs 성장 17권 |
| 진행률 78%는 **되살리지 않고 삭제** | 전체 쪽수를 안 받으니 계산할 수 없다 |
| **새 재료 ⑤** 확인 장치 먼저 심기 | |

**남길 원칙:** 같은 정보를 두 군데 적지 않는다. / 가짜 숫자를 만나면 선택지는 셋이다 — 계산한다 / 없앤다 / 계산에 필요한 입력을 새로 만든다.

### 3차시 — NFC와 방해금지

| 순서 | 내용 |
|---|---|
| 1 | **새 재료 ①** 자동재생 정책으로 "한계 → 우회" 를 쉬운 것으로 먼저 경험 |
| 2 | 웹은 방해금지를 켤 수 없다. 왜 막았는지 반대로 생각해보기 |
| 3 | 층 나누기: `[NFC 태그] → [폰 자동화] → [우리 웹앱]` |
| 4 | 단축어 만들기 + 태그 쓰기 (`폰-자동화-설정.md`) |
| 5 | **해제 규칙 만들기 (필수)** |
| 6 | **새 재료 ②** 그 해제 버튼이 실패하면? |

**남길 원칙:** 안 되는 데는 보안상 이유가 있다. / 다 만들 필요 없다. / 상태를 바꾸면 되돌리는 방법도 만든다.

### 4차시 — 배포와 발표

| 활동 | 재료 |
|---|---|
| 배포 (파이어베이스 또는 Netlify) | **새 재료 ③** 파일 개수 세기, `/.git/HEAD` 열어보기 |
| 고쳤는데 안 바뀔 때 | **새 재료 ④** 캐시 |
| manifest.json → 홈 화면 추가 | 아이콘이 생긴다. 단, "앱처럼 보이는 웹"이다 |
| **새 재료 ⑥** 아이폰 저장소 분리 | 설계의 대가. 내보내기/가져오기로 대응 |
| 발표 리허설 | 한계를 **먼저** 말한다 |

---

## 수업 중 사고 대비

| 증상 | 먼저 볼 것 |
|---|---|
| 고쳤는데 폰에서 안 바뀜 | 캐시. 탭 닫았다 열기, 주소 뒤에 `?v=2` |
| 저장이 안 되는 것 같음 | 보관함 맨 위 `저장된 기록 N개` 줄 |
| 저장했는데 목록에 없음 | 샘플 책(데미안/아몬드/모모/어린 왕자) 이름인지 확인 |
| 소리가 안 남 | 화면을 한 번 터치했는지 |
| 홈 화면 앱이 비어 있음 | iOS 저장소 분리. 사파리에서 확인 |
| 폰에서 주소가 안 열림 | 같은 와이파이인지 / HTTPS 주소인지 |

---

## 발표 때 학생이 말할 한 문장

> **"웹으로는 방해금지를 켤 수 없다는 걸 알게 되어서,
> 휴대폰 자동화와 웹앱을 연결하는 방식을 선택했습니다."**

한계를 먼저 말하고 우회로를 설명하면 질문받을 게 없어진다.
"앱을 만들었습니다"가 아니라 **"웹앱을 만들었습니다"**.

예상 질문과 답은 `고친-버그-목록.md` 끝에 있다.
마지막 질문("AI로 만든 거 아닌가요")이 핵심이고, 거기에 구체적으로 답할 수 있으면 나머지는 다 통과한다.
